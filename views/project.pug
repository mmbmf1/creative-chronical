style.
  @keyframes fadeIn {
    0% {
      opacity: 0;
    }
    100% {
      opacity: 1;
    }
  }
  .fade-in {
    animation-name: fadeIn;
    animation-duration: 2s;
  }

.py-20
  .w-full.flex.flex-row.justify-center
    i.ph.ph-caret-left.arrow.self-center.mr-10.cursor-pointer.text-gray-300.text-5xl.font-extrabold(
      class='hover:scale-110',
      hx-get=`/api/project?id=${prev_id}&prev=`,
      hx-target='#container'
    )
    #image(class='w-[40rem] h-[30rem]')
      img.w-full.h-full.rounded-md.cursor-pointer.fade-in(
        class='hover:scale-105',
        src=image_url,
        hx-get=`/api/details?id=${id}`,
        hx-target='#image'
      )
      p.w-full.mt-4.text-gray-500.text-center.text-3xl #{ name }
    i.ph.ph-caret-right.arrow.self-center.ml-10.cursor-pointer.text-gray-300.text-5xl.font-extrabold(
      class='hover:scale-110',
      hx-get=`/api/project?id=${next_id}&next=`,
      hx-target='#container'
    )
